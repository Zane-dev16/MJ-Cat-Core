stages:
  data_ingestion:
    cmd: python src/data_ingestion.py
    deps:
      - src/data_ingestion.py
      - params.yaml
    outs:
      - data/dataset:
          type: directory
          persist: true
    params:
      - s3_bucket
      - s3_prefix

